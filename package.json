{
  "name": "evil-entity",
  "displayName": "EvilEntity",
  "version": "0.0.1",
  "engines": { "vscode": "^1.80.0" },
  "activationEvents": ["onCommand:evil.start"],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      { "command": "evil.start", "title": "Spawn the Evil Entity" },
      { "command": "evil.stop", "title": "Delete the Evil Entity" }
    ]
  }
}
